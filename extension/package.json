{
  "name": "sentinel-transaction-guardian",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sentinel Transaction Guardian — Chrome MV3 extension, TypeScript, bundled with esbuild. Reports into the local Sentinel Agent (see ../agent).",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.4"
  }
}
